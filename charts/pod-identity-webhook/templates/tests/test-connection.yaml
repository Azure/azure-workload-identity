apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pod-identity-webhook.fullname" . }}-test-connection"
  labels:
    {{- include "pod-identity-webhook.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pod-identity-webhook.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
